%slide
  %hgroup
    %h2
      Framework - Template
  %article
    %p Use new framework items for template
    %p.blue3 The #load helper method injects components
    %br
    %br
    %pre.prettyprint{'data-lang' => 'ruby'}
      :preserve
        SparkleFormation.new(:tester).load(:base).overrides do
          dynamic!(:node, :first)
          # Now reusable
          dynamic!(:node, :second)
        end
